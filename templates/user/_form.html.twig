{{ form_start(form) }}<div class="row">
    <div class="col">
    {{ form_row(form.firstname) }}
    </div>
    <div class="col">
    {{ form_row(form.lastname) }}
    </div>
    <div class="col">
    {{ form_row(form.email) }}
    </div>
    <div class="col">
    {{ form_row(form.adress) }}
    </div>
    <div class="col">
    {{ form_row(form.phone) }}
    </div>
    <div class="col">
    {{ form_row(form.speudo) }}
    </div>
</div>
    

    {# {% if form.vars.value.image != "" %}
        <p> Image actuelle:</p>
        <img class="img-size-little" src="/image/upload/{{form.vars.value.image}}" alt="image">
    {% endif %}
    {# <pre>
    {{ dump(form.vars.value.image)}}
    </pre> #}
    {#{{ form_row(form.image) }} #}


    <button class="btn btn-warning">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
