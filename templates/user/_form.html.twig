{{ form_start(form) }}<div class="row">
    <div class="col">
    {{ form_row(form.firstname) }}
    </div>
    <div class="col">
    {{ form_row(form.lastname) }}
    </div>
    <div class="col">
    {{ form_row(form.email) }}
    </div>
    <div class="col">
    {{ form_row(form.adress) }}
    </div>
    <div class="col">
    {{ form_row(form.phone) }}
    </div>
    <div class="col">
    {{ form_row(form.speudo) }}
    </div>
</div>
{{ form_row(form.password) }}
    {% if form.vars.value.picture != "" %}
        <p> Image actuelle:</p>
        <img class="img-size-little" src="/image/upload/{{form.vars.value.picture}}" alt="image">
    {% endif %}
    {# <pre>
    {{ dump(form.vars.value.image)}}
    </pre> #}
    {{ form_row(form.picture) }}

    


    <button class="btn btn-warning">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
