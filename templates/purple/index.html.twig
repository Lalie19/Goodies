{% extends 'base.html.twig' %}

{% block title %}Purple Jacinth{% endblock %}

{% block body %}
    <h1>Goodies index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for goody in purples %}
            <tr>
                <td>{{ goody.id }}</td>
                <td>{{ goody.name }}</td>
                <td>{{ goody.description }}</td>
                <td>{{ goody.price }}</td>
                <td> 
                    <a href="{{ path('goodies_show', {'id': goody.id}) }}">show</a>
                    <a href="{{ path('goodies_edit', {'id': goody.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
